import javax.swing.ImageIcon;

public class Laban extends Sprite {
	
	//Take the constructor from our superclass(Sprite) and load the specific Laban image. 
	public Laban() {
		super();
		image = new ImageIcon("laban.png");
	}
}