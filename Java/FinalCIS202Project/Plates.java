import javax.swing.ImageIcon;

public class Plates extends Sprite {
	
	//Take the constructor from our superclass(Sprite) and load the specific Plates image. 
	public Plates() {
		super();
		image = new ImageIcon("plates.png");
	}
}